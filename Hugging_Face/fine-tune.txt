pip install huggingface_hub
huggingface-cli login
curl -s https://packagecloud.io/install/repositories/github/git-lfs/script.deb.sh | sudo bash
sudo apt-get install git-lfs

git clone https://huggingface.co/Rubens/modelo-XXX
VS open folder modelo-voice
cd folder
gsutil cp -r gs://hugging-XXX/checkpoint-1000-XXX/* ./

select:
preprocessor_config.json
special_tokens_map.json
tokenizer_config.json
vocab.json
config.json
pytorch_model.bin

git add . && git commit -m "Add model files" && git push

Model card = readme.md
What kind of model is it?
What is your model useful for?
What data was your model trained on?
How well does your model perform?

git config --global user.name "username"

git config --global user.email "email@mail.com"

rm -rf .git

git init

git add README.md

git config --global http.postBuffer 524288000

git remote add origin https://github.com/RubensZimbres/TestePush

git pull

git status

git add . && git commit -m "Add model files" && git push
